fpdf2
openpyxl>=3.1.0

# zoneinfo has no tz database on Windows (and on slim Linux images without
# tzdata), so report timestamps silently fell back to UTC while labelled IST.
tzdata>=2024.1
# QR encoding for fire-asset stickers (services/fire_qr.py). Pure Python,
# zero dependencies, no network calls at encode time — airgapped-safe.
segno==1.6.6
